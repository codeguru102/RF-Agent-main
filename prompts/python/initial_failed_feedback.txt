Your generated reward function is:
{reward_function}

It met a validation or execution error:
{traceback_msg}

Please fix the bug and provide a reward function that compiles and matches the configured task signature.
First, provide the structured design_thought with the required five sections: Weaknesses of Previous Reward Function, Improvements, Reward Terms, Combined Effect, and Final Reward Function.
Next, write the corrected reward function code.

Return exactly one JSON object with keys "design_thought" and "reward_code".
The reward_code value must contain the complete Python function with the configured reward signature.
Do not give additional explanations outside the JSON object.
