First, write a concise but detailed design thought for the new reward function.
The design_thought value must be Markdown text with these exact sections:

### 1. Weaknesses of Previous Reward Function
Briefly explain what was wrong with the previous reward function and what behavior it failed to encourage or discourage.

### 2. Improvements
Briefly explain what the new reward function improves and why the changes should work better.

### 3. Reward Terms
For each reward term, list: Name, Formula / expression, Expected range before weighting, Coefficient, Weighted range, Status (Added, Modified, Removed, or Unchanged), and a short purpose.

### 4. Combined Effect
Briefly explain how the terms work together and what behavior the overall reward is expected to encourage.

### 5. Final Reward Function
State that the complete implementation is provided in reward_code.

Keep each section concise: use 1-3 sentences per section, except Reward Terms may use a compact bullet list or table.
Focus on actionable reasoning, not lengthy analysis.
Next, write a complete MATLAB reward function based on this design.
In the reward_code, add concise comments for every reward component that state its expected raw range and weighted range.
Also add a concise comment beside the final combined reward calculation that states the expected total reward range.

Output exactly one JSON object:
{
  "design_thought": "Markdown design note using the required five sections",
  "reward_code": "complete MATLAB function code"
}

The MATLAB code must define reward_fcn and must be self-contained.
Do not change the configured input/output signature.
Do not call unavailable project-specific helpers.
The component output returned for logging must be a numeric array.
The reward_fcn function output component must be [reward, isdone, debug] style.
Here debug contains reward components.
Use valid field names with numeric values, for example reward_components.velocity_reward = double(velocity_reward).
Every component returned for logging must have a nearby comment describing its expected range.
Do not give additional explanations outside the JSON object.
The generated candidate will be saved as reward_fcn.m.
