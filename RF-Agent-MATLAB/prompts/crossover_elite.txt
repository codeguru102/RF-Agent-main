I have {nums} existing reward functions with their design ideas, codes, scores, actions, and offline training feedback as follows.

{reward_func_group}

Please create a new reward function inspired by those reward functions. Try to list the common ideas in high-score reward functions internally, combine high-performing reward components from different reward functions based on the tracked data, avoid contradictory terms, and keep the final reward simple enough for stable training. The new reward function should have a higher task score.
