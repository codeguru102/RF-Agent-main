Create a reward that explores a meaningfully different design direction from these previous candidates.

Candidate group:
{reward_func_group}

The new reward should still use the task variables and optimize the same Python training score, but should not be a small parameter tweak.
