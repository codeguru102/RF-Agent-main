I have one reward function with its design idea and code as follows.

Design Idea:
{design_thought}

Code:
{reward_function}

We trained an RL policy using the provided reward function code and tracked the values of individual reward components as well as global policy metrics from the offline logs:
{trained_results}

Please identify the reward function parameters, scales, temperatures, tolerances, bonuses, and penalties, then create a new reward function with different parameter settings compared to the provided version. Preserve the main mechanism when it is useful. The new reward function should have a higher task score.
